
############ Requirements for the SolReSol Translator ############

thesaurus >= 0.2.2
pysine >= 0.9.2
pyserial >= 3.4

# Note that pysine requires pyaudio, which may need to be installed with apt-get or brew
# Install directly from requirements.txt with pip install -r requirements.txt